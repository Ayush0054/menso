---
name: create-evals
description: Add Menso eval cases and deterministic contract checks without executing them.
---

# Create Menso evals

Cover prompt injection, public-send exclusion, fixed executor capability, external-execution metadata, Workflow rejection, target mismatch, user isolation, and continuation-envelope preservation. Agno `Case` supports Agents/Teams, so cover Workflow wiring with deterministic contract checks or a separate authenticated integration fixture. Author cases without running them. Never make eval setup mutate real learning or native applications.
