---
name: extend-agent
description: Extend Menso behavior against the exact pinned Agno API and typed security contracts.
---

# Extend Menso

Inspect Agno `2.8.5` official source before changing framework APIs. Add semantic typed actions only when target, success evidence, expiry, and idempotency can be verified by the Mac broker. Keep authority fields deterministic and outside model control. Update schemas, workflow gates, manifest, docs, and eval proposals together. Never attach raw CUA, shell, browser, or provider secrets. Checks remain deferred until explicitly requested.
