---
name: review-and-improve
description: Review backend code, docs, configuration, and trust-boundary drift without side effects.
---

# Review backend

Statically compare registered resources, environment variables, stable IDs, package discovery, docs, schedules, and API contracts. Confirm public Menso cannot send, executors each have one external tool, the registry exposes no privileged capability, JWT is fail closed, Realtime hashes the verified subject, and portable packages import no Railway code. Remain read-only unless asked to fix. Do not run imports, validation, evals, containers, or deploys without permission.
